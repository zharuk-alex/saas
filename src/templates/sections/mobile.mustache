<section id="mobile-section" class="py-5">
  <div class="container">
    <div class="row">
      <div class="col-md-12">
        {{#mobile}}
        <h2 class="h2 mt-5 mb-2">{{heading}}</h2>
          {{#item}}
          <div class="blue-text">{{title}}</p>
          {{/item}}
        {{/mobile}}
      </div>
    </div>
  </div>
</section>
